#include <stdio.h>
#include <stdlib.h>

#include "sort.h"

/**
 * merge_sort - implementation of shell sort algorithm
 * @array: an array of positive integers to be sorted
 * @size: how many such integers there are
*/
void merge_sort(int *array, size_t size)
{
	if (!array || !size)
		return;
}
